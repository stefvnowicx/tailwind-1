module.exports = {
   content: [
      "./index.html", // Określ ścieżki do swoich plików HTML i JS
   ],
   theme: {
      extend: {
         fontFamily: {
            sans: ["Montserrat", "sans-serif"],
         },
         container: {
            center: true,
         },
      },
   },
   plugins: [],
};

// npm run build/watch
